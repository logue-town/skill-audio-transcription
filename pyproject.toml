[project]
name = "skill-audio-transcription"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "mlx-whisper",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
